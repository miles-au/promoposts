<%= form_with(model: scheduled_post, local: true) do |form| %>
  <% if scheduled_post.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(scheduled_post.errors.count, "error") %> prohibited this scheduled_post from being saved:</h2>

      <ul>
      <% scheduled_post.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
