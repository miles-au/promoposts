$("#startingFeed").hide();
var tabs = document.getElementsByClassName('tab');

for(var x = 0; x < tabs.length; x++){
	if(tabs[x].classList.contains('tab-active')){
		tabs[x].classList.remove('tab-active');
	}
}

var globalFeed = document.getElementById('globalFeed');
globalFeed.classList.add('tab-active');

var userFeedContainer = document.getElementById('userFeedContainer');
var globalFeedContainer = document.getElementById('globalFeedContainer');
userFeedContainer.classList.add('hide');
if(globalFeedContainer.classList.contains('hide')){
	globalFeedContainer.classList.remove('hide');
}

<% if @feed_items.next_page %>
  $('#globalFeedContainer').append("<%= j render('shared/feed')%>");
  $('.pagination').replaceWith("<%= j will_paginate @feed_items %>");
<% else %>
  $('.pagination').remove();
  $('#globalFeedContainer').append("<hr><h3>end</h3");
<% end %>