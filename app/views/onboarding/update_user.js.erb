<% if @errors %>
	$("#onboarding-errors").html("<%= j render 'shared/error_messages', object: current_user %>")
<% else %>
	update_stage(<%= @step %>);
<% end %>

<% current_user.onboarding_stage = -1 %>
<% current_user.save %>
<% clear_location %>