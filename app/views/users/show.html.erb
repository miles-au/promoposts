<% provide(:title, @user.name) %>

<!--modal-->
  <div id="modal-window" class="modal fade" role="dialog" aria-labelledby="myModalLabel" aria-hidden="true">
    <div class="modal-dialog" role="document" style="width: 75%;">
      <div class="modal-content"></div>
    </div>
  </div>

<% if @user == current_user %>
  <%= link_to(edit_user_path) do%>
  <span class='glyphicon glyphicon-pencil'></span><p style="display: inline-block; margin-left: 5px;">edit my profile</p>
  <% end %>
<% end %>

<div class="row">
  <div class="col-md-3">
    <section class="user_info">
      <h1>
        <div class="avatar" style="background-image: url('<%= @user.avatar %>'); height: 150px; width: 150px; background-position: center; margin: 0 auto;"></div>
        <span style="text-align: center; display: block;"><%= @user.name %></span>
      </h1>
    </section>
    <div class="stats">
      <%= render 'shared/stats' %>
      <%= render 'follow_form' if logged_in? %>
    </div>

  <div id="topTen">
      <% if @products.count > 0 || @user == current_user %>
        <h4 id="topPicksTitle">My Top Picks: </h4>
      <% end %>
        
        <%= render @products %>

        <% if @user == current_user %>
          <% if @products.count < 10 %>
            <%=link_to({:controller => "users", :action => "add_product", :user_id => current_user.id, :remote => true, 'data-toggle' =>  "modal", 'data-target' => '#modal-window', class: 'btn btn-primary btn-lg'},) do%>
              <div class="product">
                <div class="addProduct">
                  <p class="productInnerText"><span class="glyphicon glyphicon-plus" style="line-height: 150px;"></span></p>
                </div>
              </div>
              <h6 style="text-align: center; margin-bottom: 30px;">Add up to 10 products</h6>
            <% end %> 
          <% else %>
              <h6>You've reached your limit of 10 products</h6>
          <% end %>
        <% end %>
    </div>


  </div>
  <div class="col-md-9" style="height: 300px;">
    <div id="coverPhoto" style="background-image: url('<%= @user.cover_photo %>'); background-position: center; background-size: cover; height: <%= @user.cover_photo? ? '100' : '0' %>%;">
    </div>

    <% if @events %>
      <h3>Activity (<%= @events.count %>)</h3>
      <ol id="microPostsContainer" class="microposts micropost-full">
        <%= render @events %>
      </ol>
      <%= will_paginate @events %>
    <% end %>

  </div>
</div>