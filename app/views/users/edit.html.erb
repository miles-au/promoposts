<% provide(:title, "Edit user") %>
<% provide(:button_text, "Save Changes") %>
<h1>update your profile</h1>

<div class="row">
	<div class="col-md-3">
		<img class="avatar" src="<%= current_user.avatar %>" style="height: 150px; width: 150px;"/>
	</div>
	<div class="col-md-6">
		<%= render 'form' %>
	</div>
</div>