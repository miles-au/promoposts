$('#microPostsContainer').append('<%= j render @events %>');
<% if @events.next_page %>
  $('.pagination').replaceWith("<%= j will_paginate @events %>");
<% else %>
  $(window).off('scroll');
  $('.pagination').replaceWith('<hr><h3>end</h3>');
<% end %>