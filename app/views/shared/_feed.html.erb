<div class="feed">
  <ol id="postsContainer" class="microposts microposts-grid">
    <%= render @feed_items %>
  </ol>
  <% if @feed_items.any? %>
  	<%= will_paginate @feed_items %>
  <% end %>
</div>