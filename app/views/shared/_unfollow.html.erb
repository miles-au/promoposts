<%= form_for(current_user.active_relationships.find_by(followed_id: @user.id), html: { method: :delete }, remote: true) do |f| %>
  <%= button_tag(type: 'submit', class: "unfollow-btn follow-small") do %>
	<span class="glyphicon glyphicon-minus"></span><div>unfollow</div>
  <% end %>
<% end %>